# Dedicated Google OAuth Web client for the disposable local Rehearsal Auth service.
# Never reuse hosted Supabase credentials here. The authorized redirect URI is:
# http://127.0.0.1:58321/auth/v1/callback
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
